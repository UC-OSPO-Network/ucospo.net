version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:25.9.0
    steps:
      - checkout
      - run:
          name: build_website
          command: make html
      - store_artifacts:
          path: _build/html/
